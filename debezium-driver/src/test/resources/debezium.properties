response.partitions=10
response.max.backlog=10

# Producer settings
producers.metadata.broker.list=localhost:9092
producers.producer.type=sync
producers.compression.codec=none
producers.serializer.class=kafka.serializer.DefaultEncoder

# Consumer settings
consumers.zookeeper.connect=127.0.0.1:2181
consumers.zookeeper.connection.timeout.ms=1000000
