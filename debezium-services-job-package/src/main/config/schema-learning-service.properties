# Job
job.factory.class=org.apache.samza.job.yarn.YarnJobFactory
job.name=schema-learning-service

# YARN
yarn.package.path=file://${basedir}/target/${project.artifactId}-${pom.version}-dist.tar.gz

# Task
task.class=org.debezium.services.SchemaStorageService
task.inputs=kafka.schema-learning

# Serializers
serializers.registry.json.class=org.apache.samza.serializers.JsonSerdeFactory
serializers.registry.string.class=org.apache.samza.serializers.StringSerdeFactory

# Systems
systems.kafka.samza.factory=org.apache.samza.system.kafka.KafkaSystemFactory
systems.kafka.samza.msg.serde=json
systems.kafka.consumer.zookeeper.connect=localhost:2181/
systems.kafka.consumer.auto.offset.reset=largest
systems.kafka.producer.metadata.broker.list=localhost:9092
systems.kafka.producer.producer.type=sync
# Normally, we'd set this much higher, but we want things to look snappy in the demo.
systems.kafka.producer.batch.num.messages=1

# Key-value storage
stores.schema-learning-cache.factory=org.apache.samza.storage.kv.KeyValueStorageEngineFactory
stores.schema-learning-cache.changelog=kafka.schema-learning-cache-changelog
stores.schema-learning-cache.key.serde=string
stores.schema-learning-cache.msg.serde=json

# Normally, we'd set this much higher, but we want things to look snappy in the demo.
stores.schema-learning-cache.write.batch.size=0
stores.schema-learning-cache.object.cache.size=0
